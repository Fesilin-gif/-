import urllib.request
import json
import urllib.parse
from html.parser import HTMLParser

def get_image(query):
    # use duckduckgo duckduckgo html search with a user agent, duckduckgo html search has images
    # actually, I can just use a fake JSON API or wikimedia commons for these
    pass

# We can directly download specific wikipedia images since I know the URLs for some.
